{
  "devDependencies": {
    "clean-css": "^5.3.2",
    "css": "^3.0.0",
    "nodemon": "^3.0.1"
  },
  "scripts": {
    "dev": "nodemon --ext js,html,css --watch html --watch css --watch toInlineCSS --exec \"npm run run-scripts\"",
    "run-scripts": "node css.js && node build.js"
  },
  "dependencies": {
    "html-minifier": "^4.0.0"
  }
}
